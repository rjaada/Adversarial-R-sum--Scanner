# Generated run artifacts — do not commit
outputs/

# Résumé and JD input files — may contain real personal data
inputs/resumes/
inputs/jds/

# Manual review file — may contain human annotations on real résumé content
# Keep validation_template.csv (tracked as blank template); ignore the filled copy
review/validation.csv

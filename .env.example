# PostgreSQL connection string — required for DB wrapper and integration tests only.
# Pure scanner logic (scan_source_for_adversarial_signatures) needs no DB.
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

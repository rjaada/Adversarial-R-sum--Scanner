DATABASE_URL=postgresql://user:password@localhost:5432/tracerank

# Local LLM — optional. Without this the "Generate AI rewrites" button is not shown.
# Supports any OpenAI-compatible endpoint: Ollama, llama.cpp server, LM Studio.
LLM_ENDPOINT=http://localhost:11434
LLM_MODEL=llama3
LLM_TIMEOUT=30
